import React from 'react'

export default class Fail extends React.Component{
    construct(props){
        props(props)
    }

    render(){
        return (
            <div>
                fail
            </div>
        )
    }
}