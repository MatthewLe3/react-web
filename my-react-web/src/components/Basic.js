import React from 'react'

export default class Basic extends React.Component{
    construct(props){
        props(props)
    }

    render(){
        return (
            <div>
                Basic
            </div>
        )
    }
}