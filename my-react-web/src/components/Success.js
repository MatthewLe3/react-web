import React from 'react'

export default class Success extends React.Component{
    construct(props){
        props(props)
    }

    render(){
        return (
            <div>
                Success
            </div>
        )
    }
}