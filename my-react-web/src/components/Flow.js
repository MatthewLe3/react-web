import React from 'react'

export default class Flow extends React.Component{
    construct(props){
        props(props)
    }

    render(){
        return (
            <div>
                Flow
            </div>
        )
    }
}